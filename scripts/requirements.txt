# Python dependencies for the provenance-first corpus tooling.
# Deliberately minimal and mainstream, for long-term portability.
PyYAML>=6.0
jsonschema>=4.0
pymupdf==1.28.2  # extract.py reproducibility checker (PyMuPDF). PINNED, deliberately not >=:
                 # one record (un/unclos-1982) derives its text WITH PyMuP...[truncated]
